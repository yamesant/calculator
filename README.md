This project implements a simple calculator engine.

It can construct and evaluate arithmetic expressions that involve real numbers.

Supported operations include addition, multiplication, division, subtraction, as well as exponentiation, direction, standard deviation, logarithm, and their combinations.

It can also convert expressions to and from JSON and XML.